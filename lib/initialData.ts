import { Trip } from './types';

/**
 * Clean slate: No hardcoded mock trips or dummy expenses.
 * Fresh users start with an empty array and create their own trips.
 */
export const INITIAL_TRIPS: Trip[] = [];
